# Backend

NestJS REST API.

```bash
copy .env.example .env
npm install
npm run start:dev
```

Needs PostgreSQL. See the root README for DB setup.

Swagger UI: `http://localhost:5000/api/docs`

Login or register, copy `accessToken`, then click **Authorize**.
